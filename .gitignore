# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
.pnpm-store

# Testing
/coverage
__snapshots__/
jest-report.html

# Next.js
/.next/
/out/

# Production
/build

# Misc
.DS_Store
Thumbs.db
*.pem
*.key
*.cert

# Debug Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Local Env Files (can opt-in for committing if needed)
/.env
.env.local
.env.development
.env.test
.env.production
!.env.example

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
# next-env.d.ts  # Uncomment if you don't want to commit it; Next.js regenerates this file

# Editor Directories and Files
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp

# Temporary Files
*.log
*.tmp
*.bak
*.cache

# Dependency Lockfiles (optional, depending on your preference)
# package-lock.json  # Uncomment if using npm and don't want to commit the lockfile
# yarn.lock          # Uncomment if using Yarn and don't want to commit the lockfile
# pnpm-lock.yaml     # Uncomment if using pnpm and don't want to commit the lockfile

# Build Tools and Linters
.eslintcache
.prettiercache
/stylelintcache

# Documentation (if applicable)
docs/_build/
dist-docs/

# macOS-specific
.DS_Store
.AppleDouble
.LSOverride

# Windows-specific
Thumbs.db
ehthumbs.db
Desktop.ini

# VS Code-specific
.vscode-test

# JetBrains-specific (e.g., WebStorm, IntelliJ)
.idea/
*.iml

# Parcel (if used)
/.parcel-cache
/dist

# Storybook (if used)
.storybook-static/

# Cypress (if used for testing)
cypress/screenshots/
cypress/videos/

# Docker (if used)
docker-compose.override.yml
*.dockerignore

# Database (if applicable)
*.sql
*.sqlite

# Package Manager Artifacts
pnpm-lock.yaml  # Uncomment if you don’t want to commit pnpm lockfile
pnpm-store/

# Other Common Ignores
*.swp
*~
*.orig
*.rej
node_repl_history
*.pid
*.seed
*.pid.lock